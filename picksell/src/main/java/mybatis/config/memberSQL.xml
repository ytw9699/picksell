<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member"> 
	<!-- 일반 회원가입 -->
	<insert id="insertMember" parameterType="hashmap">
		insert into PS_MEMBER(id,
							password,
							name,
							email,
							address,
							kind,
							account,
							account_name,
							bank,
							regdate) values(#{id},
									#{password},
									#{name},
									#{email},
									#{address},
									#{kind},
									#{account},
									#{account_name},
									#{bank},
									sysdate )
	</insert>
	
	<select id="userCheck" parameterType="hashmap" resultType="hashmap">
			SELECT ID,PASSWORD,NAME FROM PS_MEMBER WHERE ID = #{id}
	</select>
	<!-- <select id="userCheck" resultType="hashmap" parameterType="String" >
			SELECT ID,PASSWORD,NAME FROM PS_MEMBER WHERE ID = #{ID}
	</select>
	 -->
</mapper>